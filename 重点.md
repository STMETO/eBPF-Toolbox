1.pid和tid的命名空间问题，需要找到容器内部真实的pid和tid，而不是宿主机的

2.per cpu map传回用户空间的数据，是把，八字节对齐的，所以要自己加入padding来对齐
	/*
	 * PERCPU_ARRAY lookup 会连续返回 possible CPU 的 value，每份 value 按
	 * 8 字节对齐。计数/总耗时求和；最大值则连同对应进程字段整组复制，
	 * 避免“最大耗时”和进程名来自不同 CPU。
	 *
	 * stride计算：向上对齐至8字节边界
	 * (size +7) & ~7 是业界标准向上8字节对齐算法
	 +7：不足 8 字节的部分补齐
	 & ~7：把低 3bit 清零，强制向下对齐到 8 倍数
	 */
	size_t stride = (sizeof(struct ContextSwitch_stats) + 7) & ~((size_t)7);

3.重写结构体
	tracepoint 传给 eBPF 程序的ctx是一大块原始二进制内存；我们只想拿里面其中一个字段nr_reclaimed。
	不能直接用内核头文件里真正的结构体，所以我们手写一个 “裁剪版镜像”，叫xxx___local，配合preserve_access_index，让 CO‑RE 帮我们找字段偏移。

	## 背景
	tracepoint触发时，内核把`trace_event_raw_mm_vmscan_direct_reclaim_end_template`完整结构体的裸内存指针传给eBPF，就是tracepoint函数参数`void *ctx`。
	1. `trace_event_raw_xxx_template`是内核编译时动态生成的结构体，**vmlinux.h中没有完整定义**，不能直接引用原结构体。
	2. 不同内核版本，该结构体字段顺序、padding、成员数量会变化，硬编码布局会读垃圾数据。
	3. eBPF只需要其中个别字段（本例：`nr_reclaimed`），不需要全部成员。

	struct trace_event_raw_mm_vmscan_direct_reclaim_end_template___local {
		unsigned long nr_reclaimed;
	} __attribute__((preserve_access_index));

	## 各部分含义
	1. **结构体名字**：去掉后缀`___local`，必须和内核真实BTF中的结构体名称完全一致。
	2. **后缀`___local`**：社区约定标记，**不是编译器关键字**，仅用于提示阅读者：这是本地裁剪镜像，不是内核原生结构体。
	3. **结构体内部**：只写业务需要访问的成员，成员名字必须和内核完全一致，其余字段全部删掉。
	4. **`__attribute__((preserve_access_index))`**：开启CO‑RE重定位能力。

	## 工作流程
	1. tracepoint传入`ctx`，是内核原始tracepoint结构体的裸内存首地址。
	2. 该本地镜像**不用于内存布局映射，仅作为元数据模板**，给CO‑RE提供结构体名、成员名。
	3. libbpf加载eBPF字节码阶段：查询目标内核BTF，找到真实结构体中`nr_reclaimed`的真实内存偏移，修补字节码。
	4. 运行时使用`BPF_CORE_READ(args, nr_reclaimed)`，基于已经重定位好的真实偏移，从`ctx`裸内存读取字段值，实现跨内核版本兼容。

	## ⚠️重要禁忌
	1. ❌禁止直接指针解引用：`args->nr_reclaimed`，会使用本地裁剪结构体的错误布局，读到垃圾值。
	2. ✅必须使用`BPF_CORE_READ()`读取成员。
	3. 结构体名称、成员名称必须和内核BTF完全匹配，否则查找偏移失败。
	4. 该模式多用于tracepoint的`trace_event_raw_xxx_template`；普通内核结构体（task_struct等）vmlinux.h已有BTF，一般不需要。

4.